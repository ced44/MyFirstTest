package com.example.icarjs.myfirsttest;

import android.content.Context;
import android.os.Handler;
import android.view.View;

/**
 * Created by icarjs on 21/08/14.
 * Probleme de context sur cette classe
 */
/*
public class CircleThread extends Runnable {

    Handler mHandler = new Handler();
    @Override
    public void run() {
        runDrawing(); // 1 cercle par thread
        mHandler.postDelayed(monThread, 1000);// fonction permettant de rappeler le thread/second
        Display.getInstance().postInvalidate();// permet le rafraichissement de la view par invalidation
    }
}
*/
